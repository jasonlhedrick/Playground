/*
Array.pop() removes the last element from an array and then returns
the value contained within that element.
*/

const arr = [3, 2, 1, 0, 9];

console.log(`arr before calling pop() ${arr}`);
console.log(`The return value of arr.pop() : ${arr.pop()}`);
console.log(`arr after calling pop() ${arr}`);