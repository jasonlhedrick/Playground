/* 
Array.shift() removes the first element of the calling array
and then returns that elements value.
*/

const arr = [3, 8, 1, 2];

console.log(`arr before shift: ${arr}`);
console.log(`The value returned from arr.shift() : ${arr.shift()}`);
console.log(`arr after shift: ${arr}`);
